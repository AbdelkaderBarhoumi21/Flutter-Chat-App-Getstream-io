name: flutter_chat_app_gestream_io
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.9.2
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  cached_network_image: ^3.4.1
  cloud_functions: ^6.0.4
  collection: ^1.19.1
  faker: ^2.2.0
  firebase_auth: ^6.1.2
  firebase_core: ^4.2.1
  google_fonts: ^6.3.3
  jiffy: ^6.4.4
  logger: ^2.6.2
  stream_chat_flutter_core: ^9.21.0 
  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  dart_jsonwebtoken: ^3.3.1
flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg